/**
 * Garaga ZK proof verification (stub).
 * To be connected later: verify ZK proof and public signals on-chain or via verifier contract.
 */

export function verifyGaragaProof(
  _proof: unknown,
  _publicSignals: unknown
): boolean {
  return true;
}
